extends ../layouts/master

block header
block footer

block grid-test

block content
  .container
    ul.u-margin-top-40
      each route in data.routes.filter((route) => !route.isTest)
        li.u-margin-top-10
          .button
            a.button__button(href=route.href)= route.title
    ul.u-margin-top-40
      each route in data.routes.filter((route) => route.isTest)
        li.u-margin-top-10
          .button
            a.button__button.button__button--theme--dark(href=route.href)= route.title
